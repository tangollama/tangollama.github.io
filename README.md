# tangollama.github.io

My website

## To run locally

1. Clone: `git clone https://github.com/tangollama/tangollama.github.io.git`
2. Install dependencies: `npm install`
3. Run the Gatsby Server: `npm run dev`
4. View in browser at `http://localhost:8000`
